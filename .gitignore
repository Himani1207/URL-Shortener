# Repo-wide ignores.
#
# The backend and frontend each keep their own .gitignore for tool-specific
# artefacts whose paths are anchored to that directory — backend/.gitignore has a
# rule for .mvn/wrapper, which would stop matching if it were hoisted up here.
# This file covers what is shared, or what would otherwise be missed at the root.

# ---------------------------------------------------------------------------
# Secrets — the important part
# ---------------------------------------------------------------------------
# Real credentials live in .env, which is never committed. .env.example is the
# committed template and must only ever contain placeholders.
.env
.env.*
!.env.example
*.pem
*.key

# ---------------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------------
target/
build/
dist/
*.jar
*.war

# ---------------------------------------------------------------------------
# Dependencies
# ---------------------------------------------------------------------------
node_modules/
.pnpm-store/

# ---------------------------------------------------------------------------
# Logs and caches
# ---------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-error.log*
.vite/
.cache/
coverage/

# ---------------------------------------------------------------------------
# Deployment
# ---------------------------------------------------------------------------
.vercel/

# Kept locally as the reference for what to configure on Render, but not
# published. Nothing secret is in it — every credential is a `sync: false`
# placeholder that Render prompts for — the repo simply does not advertise it.
#
# Consequence worth knowing: Render's Blueprint feature reads this file FROM
# GitHub. With it ignored, the Blueprint flow cannot see it, so the service has
# to be created manually and its environment variables set by hand.
render.yaml

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
.idea/
*.iml
.vscode/
!.vscode/extensions.json
.DS_Store
Thumbs.db
